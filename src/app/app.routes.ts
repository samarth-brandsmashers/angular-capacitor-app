import { Routes } from '@angular/router';
import { ScrollListenerComponent } from './components/scroll-listener/scroll-listener.component';

export const routes: Routes = [
    {
        path: "scroll-listener",
        component: ScrollListenerComponent,
        title: "Scroll Listener"
    }
];

